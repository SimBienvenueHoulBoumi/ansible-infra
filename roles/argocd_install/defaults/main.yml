---
# Variables définies par le playbook (chemins du chart)
# argocd_chart_path, argocd_values_file

# Valeurs de repli si group_vars/all.yml non chargé (ex. autre inventaire)
argocd_namespace: "argocd"
kube_context: "kind-dev"
k8s_validate_certs: false
argocd_helm_release_name: "argocd"
argocd_helm_wait_timeout: "10m"
